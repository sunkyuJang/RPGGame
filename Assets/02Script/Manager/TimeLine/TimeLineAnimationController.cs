using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimeLineAnimationController : MonoBehaviour
{
/*    Animator nowAnimator;

    private void Awake()
    {
        nowAnimator = transform.GetComponent<Animator>();
    }
    private void Start()
    {
        transform.parent.GetComponent<TimeLineHandler>().timeLineAnimationControllers.Add(this);
    }
    public void LoopLastAnimation(bool isTrue)
    {
        nowAnimator.SetBool("NeedLooping", isTrue);
    }*/
}
