using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using GLip;

public class ManagerHandler : MonoBehaviour
{
   /* Transform Transform { set; get; }
    public StaticManager StaticManager { private set; get; }
    public DialogueManager DialogueManager { private set; get; }
    public QuestManager QuestManager { private set; get; }
    public TouchManager TouchManager { private set; get; }
    public ItemManager ItemManager { private set; get; }
    void Awake()
    {
        Transform = gameObject.transform;
        StaticManager = Instantiate(Resources.Load<StaticManager>("Managers/StaticManager"), Transform).GetComponent<StaticManager>();
        DialogueManager = Create.GetNew<DialogueManager>("Managers/DialogueManager", Transform);
        QuestManager = Create.GetNew<QuestManager>("Managers/QuestManager", Transform);
        TouchManager = Create.GetNew<TouchManager>("Managers/TouchManager", Transform);
        ItemManager = Create.GetNew<ItemManager>("Managers/ItemManager", Transform);
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
